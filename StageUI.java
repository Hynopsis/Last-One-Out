package com.rpetersen.game;


/**
 * Created by lastr on 4/7/2016.
 */
import com.badlogic.gdx.ApplicationListener;
import com.badlogic.gdx.Gdx;
import com.badlogic.gdx.graphics.Camera;
import com.badlogic.gdx.graphics.GL20;
import com.badlogic.gdx.graphics.Texture;
import com.badlogic.gdx.graphics.g2d.Batch;
import com.badlogic.gdx.scenes.scene2d.Actor;
import com.badlogic.gdx.scenes.scene2d.Stage;
import com.badlogic.gdx.utils.viewport.StretchViewport;

public class StageUI  {//implements ApplicationListener
/*
    public class MyActor extends Actor {
        Texture texture = new Texture(Gdx.files.internal("button.png"));
        @Override
        public void draw(Batch batch, float alpha){
            batch.draw(texture,0,0);
            }
    }

    public static Stage stage;

    //public void SetCamera(Camera cam){
        //stage.getViewport().setCamera(cam);
    //}

    @Override
    public void create() {
        int width = Gdx.graphics.getWidth();
        int height = Gdx.graphics.getWidth();
        stage = new Stage(new StretchViewport(width, height));//width,height,false);
        //stage.setViewport();

        MyActor myActor = new MyActor();
        stage.addActor(myActor);
    }

    @Override
    public void dispose() {
        stage.dispose();
    }

    @Override
    public void render() {
        Gdx.gl.glClear(GL20.GL_COLOR_BUFFER_BIT);
        stage.draw();
    }

    @Override
    public void resize(int width, int height) {
        if(stage != null) {
            stage.getViewport().update(width, height, true);
        }
    }

    @Override
    public void pause() {
    }

    @Override
    public void resume() {
    }

    */
}
